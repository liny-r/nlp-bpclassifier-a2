# API keys — never commit
.env

# Large model binaries / weights (too large for GitHub)
saved_model/fasttext_model.bin
saved_model/*.bin
saved_model/**/*.safetensors
saved_model/**/*.pt
saved_model/**/optimizer.*
saved_model/**/rng_state.*
saved_model/**/scheduler.*

# Training checkpoints (intermediate; best_model.pkl is the deployment artifact)
saved_model/**/checkpoint-*/

# Claude Code memory (local tooling)
.claude/

# Old files
old/

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# macOS
.DS_Store

# Zip archives (raw data already in ECT/)
*.zip
